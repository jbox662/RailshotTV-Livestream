# OBS → RailShot stock Windows parity
#
# Goal: RailShot does what stock OBS Studio can do on Windows.
# Strategy: obs-studio/ is the behavior checklist. Reimplement in Qt/C++.
# Do NOT link libobs. No OBS branding — RailShot UI throughout.
# Third-party OBS plugins are out of scope.
#
# ============================================================================
# Already shipped (MVP depth — deepen under F1–F8 as needed)
# ============================================================================
# - Scenes / collections / studio mode / stream / record / virtual camera
# - Sources: VideoDevice, Display, Window (BitBlt), Image, Media, Text, Color,
#   Browser (WebView2), DesktopAudio, NDI stub, Scoreboard (RailShot-unique)
# - Filters: Opacity + Color Correction
# - Transitions: Cut, Fade, Slide, Fade to Black
# - Hotkeys, HTTP :8080 + thin WS :4455 (not full obs-websocket)
# - RailShot-unique: Scoreboard, Venue Mode, Sports/Billiards profile
#
# ============================================================================
# Parity backlog (execute in order)
# ============================================================================
#
# Phase F1 — Browser + Media harden — DONE
#   [x] WebView2: correct size/FPS, transparency, reload, scene lifecycle
#   [x] Browser properties: CSS, refresh, shutdown when inactive
#   [x] Media source: transport + restart-on-show (OBS-like)
#
# Phase F2 — Audio parity — DONE
#   [x] Audio monitoring (hear the mix)
#   [x] Per-source sync delay
#   [x] Device / desktop audio completeness (app/process audio → F4)
#   [x] Audio filters: gain, compressor, noise suppression/gate
#
# Phase F3 — Video filter catalog — DONE
#   [x] Crop, chroma/color key, mask, LUT/color grade
#   [x] Scale, scroll, sharpness, render delay
#
# Phase F4 — Capture parity — DONE
#   [x] Game Capture (inject-free WGC/BitBlt; not DLL graphics hooks)
#   [x] Window Capture via WGC (BitBlt fallback)
#   [x] Application Audio Capture (process loopback)
#
# Phase F5 — Output / encoder — NEXT
#   [ ] NVENC / QSV / x264 settings UI
#   [ ] Recording formats / quality
#   [ ] Replay buffer + remux
#   [ ] Stream service presets (rtmp-services)
#
# Phase F6 — Transitions catalog
#   [ ] Wipe / stinger / luma / swipe (OBS transition set)
#
# Phase F7 — Remote protocol
#   [ ] obs-websocket compatible protocol (auth + events + common requests)
#
# Phase F8 — Ship
#   [ ] Installer + windeployqt + virt-cam register + docs
#   [ ] Final RailShot UI pass (not OBS chrome)
#
